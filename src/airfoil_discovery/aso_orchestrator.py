"""
DEPRECATED — This module is a dead path.
The production ASO pipeline uses:
  airfoil_discovery.optimization.aso_orchestrator.ASOOrchestrator
which manages mesh transitions and failure recovery for the real optimizer.

This file (airfoil_discovery.aso_orchestrator.ASOFramework) was an early
standalone orchestrator prototype.  It is NOT imported anywhere in the
live pipeline and will be removed in a future cleanup release.

DO NOT import from this module.
"""
